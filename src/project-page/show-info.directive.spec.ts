import { ShowInfoDirective } from './show-info.directive';

describe('ShowInfoDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowInfoDirective();
    expect(directive).toBeTruthy();
  });
});
