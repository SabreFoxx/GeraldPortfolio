// stats.min.js is used to print fps stats on the web page
// Scripts were added in angular.json
/*
window.initFpsStats = () => {
    window.stats = new Stats();
    window.stats.setMode(0);
    window.stats.domElement.style.position = 'absolute';
    window.stats.domElement.style.left = '0px';
    window.stats.domElement.style.top = '0px';
    document.getElementById("stats-output") // #stats-output exists in draw.component.html
        .appendChild(window.stats.domElement);
    return window.stats;
};*/
